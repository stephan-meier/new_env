northwind_vault:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: 5432
      user: demo_user
      password: demo_pass
      dbname: demo
      schema: public
      threads: 4
